&НаКлиенте
Процедура ТоварыПередУдалением(Элемент, Отказ)
	Ответ = Вопрос("Вы действительно хотите удалить эти товары?", РежимДиалогаВопрос.ДаНет);
	Если Ответ = КодВозвратаДиалога.Да Тогда
		ТекСтрока = Элементы.Товары.ТекущиеДанные;
		Объект.Товары.Удалить(ТекСтрока);
		Объект.Всего = Объект.Товары.Итог("Сумма");	
	Иначе
		Отказ = Истина;
	КонецЕсли; 
КонецПроцедуры