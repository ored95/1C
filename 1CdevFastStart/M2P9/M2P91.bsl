// Общие модули + Вызов сервера
Функция ПолучитьЗВПЗП() Экспорт 

	Возврат Константы.ЗВПЗП.Получить();

КонецФункции // ПолучитьЗВПЗП()

// Модуль управляемого приложения
Процедура ПередЗавершениемРаботыСистемы(Отказ)
	Если ОбщиеФункцииСервера.ПолучитьЗВПЗП() Тогда
		ans = 0;
		ВвестиЗначение(ans, "Вы действительно хотите выйти? \n(1 - Да, 0 - Нет)", Тип("Число"));
		Отказ = (ans = 0);
	КонецЕсли;
КонецПроцедуры
